<%= form_for(@end_user) do |f| %>
  <% if @end_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@end_user.errors.count, "error") %> prohibited this end_user from being saved:</h2>

      <ul>
      <% @end_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :password %><br />
    <%= f.text_field :password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
